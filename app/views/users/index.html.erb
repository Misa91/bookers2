<%= render 'books/left', book: @book, user: @user %>

<!-- ユーザー一覧 -->
<% @users.each do |user| %>
<div>
  <p>ユーザー画像：<%= image_tag user.get_profile_image(100,100) %></p>
  <p>ユーザーネーム：<%= user.name %></p>
  <%= link_to 'Show', user_path(user.id) %>
</div>
<% end %>